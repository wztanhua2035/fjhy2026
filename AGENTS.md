# AGENTS.md — 《富甲横阳》AI 开发规范

> 本文件用于约束 ChatGPT Work、Codex 及其他 AI 编程 Agent 在本项目中的工作方式。  
> 核心目标：**准确修改、最小改动、减少无关扫描、降低上下文与计算资源消耗、保持项目长期可维护性。**

---

# 1. 项目概览

项目名称：**《富甲横阳》**

项目类型：

- 微信小游戏
- 2D 俯视角 RPG
- 模拟经营
- 探索 / 解谜 / 剧情
- NPC 互动
- 好感度 / 关系系统
- 异步弱社交
- 开放式小镇世界

视觉风格：

- 日系清新动漫风
- Q版人物
- 温馨明亮
- 小镇生活感
- 轻松、有烟火气

主要世界规模预计：

- 约 6 个区域
- 约 50 个地点
- 约 100 位 NPC
- 大量任务、事件、对话与互动内容

游戏采用：

**配置驱动（Data-Driven）优先**

原则上：

- 地图数据配置化
- NPC 数据配置化
- 对话配置化
- 任务配置化
- 商店配置化
- 道具配置化
- 数值配置化

避免将大量游戏内容直接硬编码在业务代码中。

---

# 2. 技术与部署原则

项目主要运行环境：

**微信小游戏**

服务端及远程资源主要部署：

**Zeabur**

可能包含：

- 登录鉴权服务
- 玩家存档
- 游戏数据
- 排行榜
- 在线配置
- 热更新配置
- 远程资源
- 管理后台

大型美术与音频资源原则上允许部署在远程服务器 / CDN，不应无必要打入微信小游戏主包。

项目应尽量保持：

**客户端轻量化 + 数据配置化 + 资源远程化**

---

# 3. AI Agent 总体工作原则

所有 AI Agent 必须遵守以下优先级：

**正确性 > 稳定性 > 最小改动 > 可维护性 > 性能优化 > 主动优化**

未经用户明确要求：

**不要主动扩大任务范围。**

每次任务应遵循：

> 只处理用户明确要求的功能以及完成该功能所必需的直接依赖。

发现其他问题时：

> 可以报告，但不要顺手修改。

---

# 4. 最小修改原则

修改任何功能时，优先寻找：

**最小可行修改方案。**

禁止因为一个小需求而：

- 大规模重构
- 全项目格式化
- 改目录结构
- 重写稳定模块
- 更换技术框架
- 更换状态管理方式
- 重写已有系统
- 修改无关 API
- 修改无关配置
- 修改无关 UI

例如：

用户要求：

> NPC 名字向上移动 8px。

Agent 不应该顺便：

- 重构 NPC 渲染系统
- 更换字体系统
- 调整 NPC 动画
- 修改 NPC 数据结构
- 改写 NPC Manager

只修改实现该需求所必要的代码。

---

# 5. 文件扫描限制

为了降低 AI 上下文及资源消耗：

**禁止无目的扫描整个项目。**

Agent 应首先：

1. 根据任务判断可能涉及的模块。
2. 搜索相关文件。
3. 仅读取必要文件。
4. 仅读取必要的直接依赖。
5. 完成修改后停止继续扩大搜索。

除非用户明确要求：

> 全项目检查 / 架构审查 / 全面排查

否则不要进行全仓库深度分析。

---

# 6. 大型资源目录限制

大型资源目录默认视为：

**只读 / 不扫描区域。**

例如：

```text
assets/
assets-large/
images/
textures/
maps/
audio/
music/
video/
remote-assets/
```

以及其他包含大量：

```text
.png
.jpg
.webp
.gif
.mp3
.ogg
.wav
.mp4
```

文件的目录。

如果任务与素材无关：

**禁止遍历、读取或分析这些目录。**

如只需要某一张图片：

只访问指定图片。

不要扫描同目录所有资源。

---

# 7. 地图资源限制

大型地图、地图切片、碰撞数据和场景数据通常体积较大。

除非任务明确涉及：

- 地图显示
- 地图切换
- 碰撞
- 寻路
- 场景加载
- 地图事件
- 地图资源

否则不要读取大型地图资源。

修改 NPC、任务、背包等功能时，不应重新分析全部地图。

---

# 8. 模块边界

项目应尽量保持模块化。

建议逻辑划分包括但不限于：

```text
src/core
src/player
src/npc
src/quest
src/dialogue
src/map
src/world
src/inventory
src/item
src/shop
src/relation
src/save
src/network
src/ui
src/audio
src/social

data/
config/
assets/
server/
docs/
```

如果实际目录名称不同：

以现有项目结构为准。

**不要为了匹配本文档而主动移动目录。**

---

# 9. 核心模块保护

已经稳定运行的核心模块默认视为：

**受保护模块。**

除非用户明确要求，不要修改其公共接口。

尤其包括：

- 游戏启动流程
- 微信登录
- 存档系统
- 网络通信
- 资源加载
- 场景管理
- 玩家核心状态
- 数据持久化
- 服务端接口
- 热更新机制

如果新功能可以通过扩展实现：

优先扩展。

不要重写核心系统。

---

# 10. 配置驱动原则

游戏内容优先存放于配置层。

例如 NPC：

```json
{
  "id": "npc_001",
  "name": "阿宁",
  "map": "kunyang_01",
  "position": {
    "x": 320,
    "y": 180
  }
}
```

而不是：

```ts
if (npcName === "阿宁") {
  x = 320;
  y = 180;
}
```

同样适用于：

- NPC
- 地图地点
- 对话
- 任务
- 道具
- 商店
- 好感度
- 事件
- 奖励
- 数值
- 剧情触发条件

新增大量内容时：

优先新增数据。

不要持续增加大量特殊判断。

---

# 11. NPC 系统原则

预计本项目最终会存在约 100 位 NPC。

因此 NPC 应尽量：

**数据驱动 + 通用系统驱动**

避免：

> 一个 NPC 一套代码。

NPC 通用能力可以包括：

- 名称
- 身份
- 头像
- 立绘
- 所在区域
- 所在地点
- 坐标
- 朝向
- 移动
- 巡逻
- 日程
- 对话
- 任务
- 商店
- 好感度
- 特殊事件

只有真正特殊的 NPC 才允许编写专属逻辑。

---

# 12. 对话系统原则

游戏会包含大量 NPC 对话。

禁止大量对话文本直接写入：

```ts
.ts
.js
```

文件。

优先放入：

```text
JSON
配置文件
对话数据文件
剧情数据文件
```

代码只负责：

- 读取
- 条件判断
- 分支
- 显示
- 触发事件

这样便于未来批量生成与修改剧情内容。

---

# 13. 任务系统原则

任务系统必须尽量通用化。

典型任务状态：

```text
locked
available
accepted
in_progress
completed
rewarded
failed
```

任务类型可以包括：

- 对话
- 到达地点
- 寻找物品
- 收集物品
- 交付物品
- 调查
- 解谜
- NPC互动
- 好感度
- 剧情事件
- 经营事件

不要为每一个任务创建一套完全独立的任务代码。

优先：

**通用任务引擎 + 配置数据。**

---

# 14. UI 修改原则

UI 小修改必须尽量局部处理。

例如：

- 调字号
- 调间距
- 调位置
- 调尺寸
- 修改按钮
- 增加提示
- 修改弹窗

不得因此：

- 重写整个 UI 系统
- 修改全局样式
- 改动其他页面布局
- 更换 UI 框架

如果用户要求修改某个页面：

优先仅修改该页面。

---

# 15. Bug 修复原则

修 Bug 时首先：

1. 确认复现路径。
2. 定位最可能原因。
3. 修复根因。
4. 检查直接相关功能。
5. 停止扩大修改。

禁止：

> 因为无法快速找到 Bug，就重写整个模块。

如果根因暂时无法确定：

应报告：

- 已检查内容
- 最可能原因
- 尚未确认部分

不要进行高风险大改。

---

# 16. 测试范围原则

默认：

**只测试本次修改涉及模块及其直接依赖。**

例如修改：

```text
Quest System
```

应重点测试：

```text
任务接受
任务进度
任务完成
任务奖励
相关存档
```

无需自动重新测试：

```text
所有地图
所有 NPC
所有商店
所有剧情
全部资源
```

除非：

本次修改涉及公共基础模块。

---

# 17. 构建原则

小修改后：

如果不需要完整构建即可验证，不要反复执行耗时完整构建。

根据实际项目选择：

- 类型检查
- 单模块测试
- lint
- 相关测试
- 必要构建

只有以下情况优先完整构建：

- 发布版本
- 核心模块修改
- 公共依赖修改
- 用户明确要求
- 单模块测试无法验证

---

# 18. 禁止无意义重复操作

Agent 应避免：

- 重复读取同一文件
- 重复搜索相同关键词
- 重复运行相同测试
- 修改后又重新扫描整个项目
- 为确认一个简单结果执行大量无关命令

已经获得足够信息后：

**直接执行任务。**

---

# 19. 禁止主动“顺便优化”

如果用户说：

> 修改 NPC 对话框。

禁止自动：

> 顺便优化 NPC 系统。

如果发现潜在优化点：

请在任务结束后写：

```text
可选建议：
- xxx 可以以后优化。
```

但不要实施。

---

# 20. 用户指令优先

如果用户明确要求：

> 全面检查

可以扩大范围。

如果用户明确要求：

> 重构

可以重构。

如果用户明确要求：

> 优化整个系统

可以进行系统级分析。

否则默认：

**局部任务模式。**

---

# 21. 每轮任务建议规模

一次开发任务建议包含：

**3～8 个同模块相关修改。**

例如：

```text
NPC 系统一轮：

1. NPC 名称显示
2. 任务感叹号
3. 对话距离检测
4. NPC 朝向玩家
5. 对话结束恢复巡逻
```

这是理想范围。

不要主动将一个简单任务扩展为整个系统开发。

---

# 22. 新功能开发原则

新增系统时，建议顺序：

```text
需求
↓
数据结构
↓
核心接口
↓
最小可运行版本
↓
测试
↓
扩展
```

不要一开始就实现：

> “未来可能需要”的全部功能。

优先完成：

**MVP（最小可行版本）。**

---

# 23. 防止过度工程化

《富甲横阳》虽然是长期项目，但仍属于中小型游戏项目。

禁止无必要引入：

- 微服务架构
- 复杂事件总线
- 过多抽象层
- 大型第三方框架
- 重型依赖
- 复杂设计模式

如果简单方案足够：

选择简单方案。

---

# 24. 微信小游戏特别要求

开发时注意：

- 微信小游戏包体
- 分包
- 资源大小
- 网络请求
- 内存
- 手机性能
- 触摸操作
- 不同屏幕比例
- 微信生命周期
- 前后台切换
- 登录状态
- 网络异常

避免完全按桌面浏览器游戏逻辑设计。

---

# 25. 性能原则

优先避免：

- 每帧创建大量对象
- 每帧 JSON 解析
- 高频 DOM/Canvas 重建
- 大量重复网络请求
- 同时加载全部 NPC
- 同时加载全部地图
- 同时加载全部资源

地图、NPC、资源应尽量：

**按区域 / 场景按需加载。**

---

# 26. 远程资源原则

如果项目已经支持远程资源：

新增大型：

- 图片
- 音乐
- 地图
- 动画
- 视频

优先考虑：

**远程资源服务器 / CDN。**

不要无必要塞入小游戏主包。

但：

不要为了远程化一个小资源而增加复杂系统。

---

# 27. 网络请求原则

客户端不得高频无意义请求服务器。

适合服务端的数据：

- 玩家账户
- 云存档
- 排行榜
- 公共世界状态
- 异步玩家互动
- 在线活动
- 后台配置

本地即可处理的内容：

不要强制请求服务器。

---

# 28. 存档兼容原则

任何涉及玩家存档的数据结构修改：

必须考虑旧版本兼容。

优先使用：

```text
新增字段 + 默认值
```

避免直接删除或改变旧字段含义。

如果必须进行迁移：

应提供明确迁移逻辑。

---

# 29. 服务端修改保护

修改服务器 API 时：

首先确认是否会影响现有客户端。

禁止未经确认：

- 删除 API
- 改接口路径
- 改字段名称
- 改响应结构
- 改鉴权方式

如果必须修改：

优先保持向后兼容。

---

# 30. Git / 版本控制原则

未经用户明确要求：

不要：

- 删除分支
- force push
- reset --hard
- 大规模回滚
- 清理未提交文件
- 删除用户本地改动

修改时尊重现有 Git 状态。

不要覆盖用户尚未提交的工作。

---

# 31. 文件删除规则

禁止因为判断文件：

> “似乎没有用”

就直接删除。

删除任何：

- 源代码
- 配置
- 美术资源
- 数据
- 地图
- 音频

之前必须确认：

没有被项目引用。

如果无法确定：

只报告，不删除。

---

# 32. 注释原则

代码应保持必要注释，但不要生成大量解释型废话注释。

推荐注释：

```ts
// NPC 离开当前地图后停止更新，减少场景开销
```

不推荐：

```ts
// 这里定义了一个变量
const x = 1;
```

---

# 33. 文档更新规则

只有当本次修改涉及：

- 系统结构
- API
- 配置格式
- 重要开发规范
- 新模块

才更新相关文档。

简单 UI 修改无需修改大量文档。

---

# 34. PROJECT_STATUS.md

如果项目根目录存在：

```text
PROJECT_STATUS.md
```

开始大型任务前优先读取它。

它用于记录：

- 当前版本
- 已完成功能
- 正在开发功能
- 稳定模块
- 已知 Bug
- 下一阶段计划

不要为了理解项目每次重新分析全部代码。

---

# 35. 当前项目状态优先级

理解项目时建议优先读取：

```text
AGENTS.md
PROJECT_STATUS.md
README.md
package.json
相关模块文件
```

不要首先：

> 全仓库扫描。

---

# 36. AI Agent 推荐工作流程

每次收到任务：

### STEP 1

阅读：

```text
AGENTS.md
```

### STEP 2

判断任务涉及：

```text
哪个模块？
```

### STEP 3

搜索：

```text
相关代码
```

### STEP 4

读取：

```text
相关文件 + 必要直接依赖
```

### STEP 5

制定：

```text
最小修改方案
```

### STEP 6

执行修改。

### STEP 7

进行：

```text
相关模块测试
```

### STEP 8

输出修改结果。

然后停止。

---

# 37. 不需要每次执行的操作

以下操作默认不执行：

```text
全仓库扫描
全项目 lint
全项目测试
完整资源扫描
全地图加载
全 NPC 数据检查
全任务数据检查
大型依赖分析
全面代码审查
```

只有任务需要时才执行。

---

# 38. 修改结果输出格式

任务结束时尽量简洁输出：

```text
完成。

修改文件：
- src/npc/NPCManager.ts
- src/ui/NPCName.ts

完成内容：
- NPC 名称位置上移
- 修复名称缩放异常
- 不影响 NPC 对话和巡逻

测试：
- NPC 显示正常
- 对话正常
- 地图切换正常

其他发现：
- 无
```

不要输出大篇工作过程。

---

# 39. 发现其他 Bug 时

如果修改过程中发现其他 Bug：

不要主动处理。

输出：

```text
另外发现：

1. xxx
2. xxx

这些问题与当前任务无直接关系，因此没有修改。
```

等待用户决定。

---

# 40. Agent 资源节省原则

本项目长期开发时，应主动减少：

- Token 使用
- 上下文长度
- 文件读取
- 工具调用
- 重复测试
- 不必要推理

因此：

> 能通过 3 个文件解决的问题，不读取 30 个文件。

> 能通过局部测试确认的问题，不进行全项目测试。

> 能通过配置修改的问题，不修改系统代码。

> 能通过扩展解决的问题，不重构核心系统。

---

# 41. 模型使用建议

如果 Agent 支持不同能力模型：

### 轻量任务

适合较轻模型：

- 文本修改
- CSS / UI 微调
- 配置修改
- JSON 修改
- 小型 Bug
- 简单逻辑

### 中等任务

适合标准开发模型：

- 新增模块
- NPC 系统
- 任务系统
- 背包
- 商店
- 地图功能
- API 接入

### 重度任务

只有以下情况考虑高推理模型：

- 复杂架构
- 跨核心模块 Bug
- 难以复现问题
- 性能瓶颈
- 大规模重构
- 数据一致性问题

不要因为模型能力更强就默认使用最高强度。

---

# 42. 用户未明确要求时的默认行为

默认：

```text
任务范围：局部
扫描范围：局部
修改范围：最小
测试范围：相关模块
资源目录：不扫描
重构：禁止
顺便优化：禁止
删除文件：禁止
公共接口修改：禁止
```

---

# 43. 最重要的执行规则

每次任务都必须牢记：

> **只修改完成当前需求真正需要修改的内容。**

> **不要重新理解整个项目。**

> **不要扫描与当前任务无关的大型目录。**

> **不要为了“小问题”进行“大重构”。**

> **发现其他问题，只报告，不顺手修改。**

> **优先配置驱动，避免硬编码。**

> **保护已经稳定运行的模块。**

> **修改完成、验证通过后立即停止继续探索。**

---

# 44. 用户指令覆盖规则

本文件属于：

**默认开发规范。**

用户在具体任务中的明确指令拥有更高优先级。

如果用户明确要求：

```text
全面排查
整体重构
全项目检查
扫描全部资源
批量修改
```

则可以暂时突破相应限制。

但只在该次任务有效。

---

# 45. 项目长期目标

《富甲横阳》应保持：

**模块化**

**配置驱动**

**轻客户端**

**可持续扩展**

**便于 AI 协作开发**

**便于持续增加地图、NPC、剧情与任务**

而不是随着内容增加不断堆积特殊代码。

最终目标：

> 增加一个 NPC，主要增加配置。

> 增加一个任务，主要增加配置。

> 增加一个地点，主要增加配置。

> 增加一段剧情，主要增加配置。

而不是每增加一项内容，都必须修改核心程序。