# 白石街五座建筑室内美术工程规格 V1

本规格对应 `packages/game-config/index.ts` 的 `interiorZones`、五个 `SceneConfig`、`baishiPlots` 和核心 NPC 配置。这里的矩形均为连续 tile 坐标 `(x, y, width, height)`；原点在背景左上角，X 向右，Y 向下。单 tile 为 32×32 世界像素。五张正式背景均为 **768×640 px**（24×20 tile），保持原尺寸与左上角锚点；人物、交互提示和出口按钮不烙入背景。

## 已接入资源 V1

- `INTERIOR_B_SALON`：`scene-layers/baishi/interiors/interior_salon_v1.png` / `interior_salon_fg_v1.png`
- `INTERIOR_B_GROCERY`：`scene-layers/baishi/interiors/interior_grocery_v1.png` / `interior_grocery_fg_v1.png`
- `INTERIOR_B_TRADE`：`scene-layers/baishi/interiors/interior_trade_v1.png` / `interior_trade_fg_v1.png`
- `INTERIOR_B_CLOTH`：`scene-layers/baishi/interiors/interior_cloth_v1.png` / `interior_cloth_fg_v1.png`
- `INTERIOR_B_INN`：`scene-layers/baishi/interiors/interior_inn_v1.png` / `interior_inn_fg_v1.png`

背景由共享 `baishiInteriorArtRegistry` 读取；每座都有透明前景图层，按 `foreground.occlusionFrontY` 与角色脚底 Y 深度排序。碰撞、入口、出口、NPC 与服务点仍只来自现有场景配置。

所有室内沿用 `spawn=(12,15)`；内部出口 portal 在 `(12,18)`，出口语义区 `(11.3,17.3,1.4,1.4)`。由街道入口进入时使用同一合法 spawn；由内部出口返回街道时使用对应外部门口的 X 与门口 Y+1。主要行走走廊保留在中央 `x≈10–14, y≈11–18`；NPC、门与柜台前至少保留一条可绕行通路。实体矩形已进入 `scene.collision`，正式美术的可行走表现必须与这些占地一致。室内人物视觉倍率为室外的 1.43 倍，但脚底世界位置和碰撞不变。

## 横阳客栈（一楼）

- Scene：`INTERIOR_B_INN`；24×20 tile；背景 768×640 px。
- 外部门：`ENT_BAISHI_INN_S`，白石街 `(8.5,20)`；内部入口 `(12,15)`；出口 `EXIT_B_INN` `(12,18)`；返回白石街 `(8.5,21)`。
- NPC：陈掌柜 `NPC_001` `(8,9)`，原有巡逻点 `(10,9)`。柜台 `INN_COUNTER=(6,10.3,5,0.8)`；对话服务区 `INN_SERVICE=(8,12,1.5,1)`。
- 临时房正式小门位于背景左上，视觉中心约像素 `(96,230)`；语义区 `INN_GUEST_ROOM=(1.8,5.1,2.6,3.2)`。大厅原有实体碰撞本轮保持不变，等候区仍为 `(15,12,6,3)`。
- 实体家具/墙：北墙 `(2,3,20,2)`；柜台上述矩形；两段侧房隔墙上述矩形；桌椅 `(15.3,12.7,1.8,0.9)`、`(19.1,12.7,1.8,0.9)`；楼梯 `(17.5,5.4,3.5,3.2)`。楼梯为软封锁，世界内显示“二楼装修中”；目前没有二楼场景或入口。
- 可烙入背景：地板、墙面、侧房轮廓、柜台后固定装饰、楼梯固定底座。建议独立 foreground：柜台前沿、楼梯扶手、桌椅上缘。中央大厅通路、侧房南侧入口、陈掌柜柜台前和 `(12,15)→(12,18)` 保持无实体装饰遮挡。
- 临时房门 portal `ENTER_INN_GUEST_ROOM=(3,7.2)` 对准背景左上小门，入房 spawn `(7,8.4)`；从房间进入大厅落在门外 `(3,8.6)`。两端落点均与对应 trigger 间隔大于 1.1 tile；开场对话未完成前大厅出口保持锁定。

## 横阳客栈主角临时房

- Scene：`INTERIOR_B_INN_GUEST_ROOM`；14×12 tile，32 px/tile；背景与透明前景均为 448×384 px，左上角锚定。背景 `scene-layers/baishi/interiors/interior_guest_room_v2.png`，前景 `interior_guest_room_fg_v2.png`。
- 入场 spawn `(7,8.4)`；下方中央门 `EXIT_INN_GUEST_ROOM=(7,10.2)` 通往大厅左上门外 `(3,8.6)`。主通路沿 `x≈7` 延伸至房间中央；四件家具交互点均可从 spawn 到达。
- 左侧床 `GUEST_BED=(1.8,3.25,2.65,3.85)`，交互点 `(5.2,5.8)`；上方中间衣柜 `GUEST_WARDROBE=(6.25,2,2,2.45)`，交互点 `(7.25,5.15)`；右上书桌 `GUEST_DESK=(9.4,3,2.6,2.4)`，交互点 `(8.9,5.9)`；右下大箱子 `GUEST_CHEST=(10.15,6.7,2.55,2.05)`，交互点 `(9.45,8.2)`。四件家具保持独立碰撞与接近区。
- 北墙 `(0.7,0.5,12.6,0.7)`、左右墙 `(0.7,1.2,0.5,8.7)` / `(12.9,1.2,0.5,8.7)`、南墙两侧 `(0.7,9.9,5,1.1)` / `(8.5,9.9,4.9,1.1)`；南中央保持门洞。地板、墙、四件家具烙入背景；前景图层为空透明保留位。不得在入口和家具接近点追加实体装饰。

## 街坊杂货铺

- Scene：`INTERIOR_B_GROCERY`；24×20 tile；背景 768×640 px。
- 外部门：`ENT_BAISHI_GROCERY_S` `(21,20)`；内部入口 `(12,15)`；出口 `EXIT_B_GROCERY` `(12,18)`；返回 `(21,21)`。
- NPC：店员 `NPC_GROCERY_CLERK` `(12,9)`；柜台 `(9.5,10.3,5,0.8)`；未来查看/购买点 `GROCERY_SERVICE=(11,12,2,1)`。
- 实体家具/墙：北墙 `(2,3,20,2)`；柜台上述矩形；货架 A `(4.2,8,2.1,4)`、货架 B `(17.7,8,2.1,4)`、货架 C `(18,13,2,1.2)`。商品展示语义区 `(4,13,3,2)`，不是新的可碰撞货架。
- 可烙入背景：地面、墙面、柜台后包装与墙面陈列。建议独立 foreground：柜台前沿和货架上缘。中轴服务区、货架之间及入口至出口通道留空，不得用额外美术把玩家夹住。

## 白石商行

- Scene：`INTERIOR_B_TRADE`；24×20 tile；背景 768×640 px。
- 外部门：`ENT_BAISHI_TRADE_S` `(32.5,20)`；内部入口 `(12,15)`；出口 `EXIT_B_TRADE` `(12,18)`；返回 `(32.5,21)`。
- NPC：伙计 `NPC_TRADE_CLERK` `(12,9)`；主柜台 `(9.5,10.3,5,0.8)`；交易区 `TRADE_SERVICE=(11,12,2,1)`。现有买卖与“第一桶金”仍在原 NPC/商店逻辑中。
- 实体家具/墙：北墙 `(2,3,20,2)`；柜台上述矩形；左仓储 `(4,7.5,2.4,3)`、右仓储 `(18,7.5,2.4,3)`。展示区 `(18,12,3,2)`、未来委托预留 `(4,13,3,2)` 均为语义区，不建立新业务。
- 可烙入背景：地板、墙面、仓储固定基座。建议独立 foreground：柜台前沿、较高货堆前缘。中央交易动线和两侧绕行空间保持可走，不覆盖 `(12,15)` 与出口。

## 青丝美发室

- Scene：`INTERIOR_B_SALON`；24×20 tile；背景 768×640 px。
- 外部门：保留既有 ID `ENT_BAISHI_SALON_W`，实际现有方向为 south，位置 `(44,20)`；内部入口 `(12,15)`；出口 `EXIT_B_SALON` `(12,18)`；返回 `(44,21)`。本轮不改旧 ID。
- NPC：美发师 `NPC_SALON_HAIRDRESSER` `(8,9)`；服务点 `SALON_SERVICE=(10.5,10,2,1)`。此处仅预留未来 Hair 选择，不开发服务商城。
- 实体家具/墙：北墙 `(2,3,20,2)`；镜台 `(4.5,5.2,5,1)`；理发椅 `(8.8,11.2,1.4,1.5)`；服务柜 `(15.5,9.5,3,0.8)`。等候区 `(17,12,4,3)` 为非实体语义区。
- 可烙入背景：地面、墙面镜台、服务柜固定后部。建议独立 foreground：理发椅靠背、镜台下缘、服务柜前沿。理发椅右侧保留前往美发师的通路；等候区与出口不加实体阻挡。

## 春衫衣坊

- Scene：`INTERIOR_B_CLOTH`；24×20 tile；背景 768×640 px。
- 外部门：`ENT_BAISHI_CLOTH_S`，南向 `(34,37.6)`；内部入口 `(12,15)`；出口 `EXIT_B_CLOTH` `(12,18)`；返回 `(34,38.6)`。
- NPC：春衫掌柜 `NPC_CLOTH_SHOPKEEPER` `(16,9)`；柜台 `(13.5,10.3,5,0.8)`；柜台对话区 `CLOTH_COUNTER_SERVICE=(15,12,2,1)`。
- 实体家具/墙：北墙 `(2,3,20,2)`；柜台上述矩形；左服装陈列架 `(4.3,7.2,2,4)`、右陈列架 `(20,7.2,2,4)`。左展示区 `(5,12,3,2)` 与试衣/换装预留区 `(18,12,3,2)` 为非实体语义区。陈列内容按完整 **Outfit** 设计，不拆上衣、裤、鞋。
- 可烙入背景：地面、墙面与固定展示架背板。建议独立 foreground：柜台前沿、衣架横杆及试衣帘前缘。入口、柜台服务区、中央通路和出口保持无遮挡；不得烙入固定人物。

## 交付边界

所有位置以当前配置和自动通路测试为准。正式背景可表现墙、地板和家具，但不能单凭视觉改变实体占地；需要遮挡人物的前缘以后制作独立 foreground。五张背景不要加入 NPC、任务文字、商品按钮或额外碰撞。本轮语义区仅用于稳定定位和制作素材，Hair、Outfit、委托、客栈二楼等业务尚未启用。
