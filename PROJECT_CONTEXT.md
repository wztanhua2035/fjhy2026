# 富甲横阳长期开发交接记录

最后更新：2026-09-09（北京时间）。本文件用于记录已确定的设计、实际开发与部署进度、已解决问题及下一阶段验收目标。以后接手开发时，先读本文件、README 和相关代码；每完成重要阶段，都同步更新本文件。

当前结论：第一阶段原型已在 Zeabur 测试服运行，已实测创建角色、入住客栈和进入白石街。下一步优先完成“客栈 → 白石街 → NPC → 商店 → 买卖 → 理发/换装 → 存档重登”完整可玩闭环，再扩地图、任务和 NPC。生产环境尚未部署。

## 1. 信息来源与可信边界

- 本地项目：`D:\ChatGPTProjects\fjhy`。
- 完整设计规格：`D:\ChatGPTProjects\参考资料\富甲横阳_微信小游戏完整开发脚本与技术实现方案_V2.2.docx`。V2.1 同目录保留。参考资料实际在项目的同级目录，并非当前仓库内部；README 中的相对路径描述需注意这一差异。
- 本次已读取 V2.2 正文、README、`docs/STATUS.md`、`docs/DEPLOYMENT.md`、`docs/WECHAT.md`、`docs/API.md`，并核对世界配置、游戏规则、API、持久化、Worker、seed 和 Dockerfile 等代码。
- 测试服部署、重启及浏览器试玩进展依据用户本次确认和历史会话《设计小游戏架构〈富甲横阳〉》（会话 ID：`6a9b604d-b40c-83e8-9d7d-3557dd9e0a95`）。本次仅做文档与代码核对，没有重新访问云控制台、调用在线健康接口或执行试玩，因此这些是最近已确认状态，并非本次实时巡检结果。
- `docs/STATUS.md` 的 2026-09-05 交付快照以及 `docs/DEPLOYMENT.md` 的“尚未推送/未创建云服务”等说明已落后于后续测试服进展；当前部署状态以本文件第 4 节为准。旧文档仍可用作操作参考，但不能直接当成当前事实。
- V2.2 描述最终要做成什么；本文件描述目前做到哪里。设计约定、已有实现、实环境验收三者必须区分，不能将配置占位或自动化测试当成完整玩法及微信真机验收。

## 2. 项目定位与架构原则

《富甲横阳》是一款微信小游戏，采用 2D 俯视角、日系清新 Q 版风格，结合 RPG、模拟经营和异步弱社交。玩家从横阳镇客栈中的普通旅人起步，通过跑商、小镇生活与后续地产经营逐渐成长，长期预留多城镇、跨城贸易和商号网络。

- 客户端解释和呈现世界，服务端裁决和保存世界，后台编辑并发布世界，CDN 提供素材。
- 货币、背包、交易、外观所有权、营业状态及后续产权、奖励均以服务端为权威。经济写入须有事务、账本和 requestId 幂等保护。
- 场景、道路、Plot、Building、NPC、外观与任务采用稳定 ID。显示名称可以修改，内部 ID 不随之变化；地块与建筑分离，不把商店硬画进基础地图。
- 弱社交使用真实玩家的 Ghost/Shadow 公开快照，不做实时多人坐标同步、碰撞和自由聊天。主角与 Ghost 共用外观渲染器。
- 热更新目标是数据、资源和规则配置；不设计任意远程 JavaScript 执行，新底层能力通过正式客户端版本更新。
- 《富甲横阳》与《坡南寻宝记》是独立项目，不混用代码或开发状态。

## 3. V2.2 已确定的设计及当前落地范围

### 世界与道路

世界采用 World → Town → District/Area → Scene 分层，通过 Portal 连接多个场景，避免一张超大 TileMap。基础 Tile 为 32×32px，场景建议 80×80 至 160×120 Tile。先画道路和自然边界，再划 Plot，最后挂建筑。

总体规划以横阳镇城区为中心，外围包括北部凤湖公园、西北鸣山村、东南坡南老街、东部东门山、西南九凰山；这些是未来独立可探索区域，不仅是背景。

道路锁定约定：

- 白石街：镇中心东西主街，与人民路、汇水河路、解放街相交；向东穿过平瑞路后直接进入东门山。
- 解放街：偏东的南北城市轴；南经白石街、县前街至通福门，再进入坡南老街；北经雅河路、人民路北段后继续北延，连接凤湖公园南侧。
- 雅河路：白石街北侧东西路，西接人民路，中经汇水河路、解放街，东接平瑞路。
- 汇水河路：人民路东侧的南北路，与雅河路、白石街相交并连接县前街。原 L 形向东的东西段已独立命名为县前街。
- 县前街：白石街南侧东西路，西接人民路，中经汇水河路、解放街，东接平瑞路；县政府规划在北侧。
- 人民路：镇西南北主公路；北段向东形成北部交通走廊，人民路北段以北为凤湖公园。
- 平瑞路：全镇最东侧南北主公路，地图上必须明显标出；与北部走廊、雅河路、白石街、县前街相交，东侧为东门山。
- 兴良路：统一使用此名称，承接人民路北段向东的东北通道，跨越平瑞路继续向东/东北，预留外部区域接口。
- 联东路：仅保留东北片区次干路配置位，具体线形尚未锁定，不得替代兴良路的主交通角色。

当前代码在 `packages/game-config/index.ts` 保留道路连接配置，第一阶段校验禁止修改道路拓扑。可玩内容仅有白石街一段 `STREET_BAISHI_01`（80×80）、10 个 Plot、5 栋建筑及其 5 个室内场景：横阳客栈、街坊杂货铺、白石商行、青丝美发室、春衫衣坊。其余道路和外围区域尚未成为可进入地图；不得把道路 ID 的存在当成全城已完成。

### 北京时间昼夜与营业时间

全世界统一按北京时间 `Asia/Shanghai`（UTC+8）的真实时间运行，由服务端判定，不跟随玩家设备时区：

- 06:00–08:59：清晨。
- 09:00–16:59：日间。
- 17:00–19:59：傍晚，仍属主要营业期。
- 20:00–23:59：夜晚，转向夜生活内容。
- 00:00–05:59：深夜，减少街道人流，保留客栈、住宅及少量夜间场所。

V2.2 普通商业建议在 19:30–20:30 左右陆续关门，行政/金融可更早，餐饮夜市可更晚；不能一刀切关掉所有内容。大多数主线/普通支线应避免严格现实时间窗。

当前规则位于 `packages/game-rules/index.ts`：营业起点包含、终点不包含，支持跨午夜；起止相同（如 `00:00` 到 `00:00`）表示全天开放。初始配置中客栈、杂货铺、白石商行全天开放，青丝美发室和春衫衣坊为 08:00–20:30。深夜进入美发室返回“店铺已打烊”是预期行为，不是进入场景失败的 Bug。

在线世界可能已通过后台发布配置，与初始 seed 不同。历史会话曾建议临时全天开放以便测试，但没有确认已执行；不要把建议写成已发布事实，也不要为测试擅改正式营业约定。

### NPC 调度与 Ghost

V2.2 规划支持 100–160 名 NPC 总库，单 Scene 通常呈现 4–8 名剧情/生活 NPC，加 3–6 名 Ghost。服务端按真实时间、地点、任务条件、优先级和权重决定出现者，客户端负责本地路线与动画，无需服务端模拟跨场景行走全过程。

规划优先级为任务 NPC 100、主线 90、功能 80、特色生活 50、普通居民 20；任务必需 NPC 应强制出现。核心 NPC 可锁名，其他名称可编辑但 ID 永久稳定。对话应支持初见/已认识等记忆。

当前实现按 enabled、sceneId、hours 过滤，按 priority 降序取最多 8 名；初始配置为客栈陈掌柜和白石街阿青、周叔、小禾，含路线数据与初见记忆。完整跨场景日程、条件权重调度、任务强制出现及路线动画仍待扩展。历史试玩记录出现“小柔”，可能源于已发布世界配置；本次未读取线上配置，不能认定名字差异是代码错误。

Worker 每 15 分钟构建近期 7 天活跃玩家的 Redis Ghost 候选池。当前 API 直接读 PostgreSQL 最新公开快照，换装时同步更新快照；加权抽样和完整生产缓存策略仍未完成。赠礼、邮箱及羁绊业务尚未开放。

### 角色创建与换装

V2.2 首次流程：选择男/女性别 → 对应性别 6 个基础形象选 1 → 调整头发、上衣、下装配色 → 四方向或 360°预览 → 确认后入住客栈。确认前免费预览；基础模板是永久身份数据，当前穿搭与基础模板分离，后续换装不能覆盖基础模板 ID。

外观按基础身体、头发前后层、上衣、下装、鞋、饰品组合，统一锚点与四方向层级，主角和 Ghost 共用渲染逻辑。美发室提供付费发型/发色服务，服装店购买后永久拥有，住宅衣柜免费切换已拥有服饰。外观用于个性、收藏与社交，不增加付费数值强度。

当前定义男女各 6 个基础形象、各 8 种发型、6 款上衣、4 款下装、4 款鞋及 6 种配色；程序绘制素材属于原型，不能当作正式美术。已实现服务端建角、初始 120 文赠款、外观保存、服饰购买、服装店切换已拥有服饰、付费理发、所有权/性别校验与 Ghost 快照同步。浏览器有操作入口；Cocos 完整换装 UI、住宅衣柜和正式美术兼容验收仍待完成。

## 4. 仓库与测试部署现状

- GitHub：`wztanhua2035/fjhy2026`，远程 `https://github.com/wztanhua2035/fjhy2026.git`，分支 `main`。
- 2026-09-09 基线落盘后，本地工作区干净，HEAD 为 `648f4e3`（`Handoff Baishi Street vertical slice`），已推送并与 `origin/main` 一致。本次仅运行白石街客户端、微信适配、任务/存档相关定向检查；未执行全项目无关测试。
- Zeabur 测试项目：`fjhy-staging`。
- 用户已确认 PostgreSQL、Redis、`game-api`、`game-worker`、`admin` 五项服务均已运行。
- API 测试域名：`https://api-fjhy-staging.wzpy.net`。
- 后台及浏览器试玩域名：`https://admin-fjhy-staging.wzpy.net`。
- 数据库 migration 和 seed 已完成；`/healthz` 正常；`/readyz` 在服务重启后仍返回 ready。此结论来自已完成测试的交接，不表示全部业务或完整重启存档流程均已验收。
- `game-api` 尚未配置正式微信 AppID/AppSecret，当前暂用 DEV 测试登录。Zeabur 项目名中的 staging 不等于程序的 `APP_ENV=STAGING`。
- 代码要求 DEV 登录同时满足 `APP_ENV=DEV`、`ALLOW_DEV_AUTH=true`、`NODE_ENV` 非 production；严格 STAGING/PROD 或 production 模式禁止 DEV 登录，并要求微信正式凭据。这里说明代码约束，没有读取或复述云端环境变量实际值。Dockerfile 默认 production；后续重建时需留意测试服运行配置与该默认值的区别。
- 生产环境尚未部署；微信正式凭据、微信正式登录验收、对象存储/CDN、远程资源缓存与热更新暂后置。不能把当前 DEV 测试服直接认定为生产上线方案。

## 5. 已解决问题与待核查事项

### Docker 与 tsx 构建启动

根据部署交接，先前阻塞服务运行的 Docker/tsx 构建问题已解决，测试服务已成功运行。当前 API/Worker 镜像以 Node.js 22 为基础，先安装依赖并生成 Prisma Client，保留 tsx 等运行所需依赖，用 `node --import tsx` 启动 TypeScript；API 构建另运行资源生成与类型检查，admin 由 Vite 构建后交给 Nginx。

后续修改安装或镜像裁剪方式时，不要遗漏 tsx/Prisma 生成步骤。本次未重新构建镜像，不补写无法确认的历史报错细节。

### Prisma 事务锁导致创建角色 500

历史根因：`pg_advisory_xact_lock` 返回 PostgreSQL void，Prisma 6 的 `$queryRaw` 无法反序列化该列，导致建角事务报错，典型信息为 `Failed to deserialize column of type 'void'`。

本地已确认 `apps/server/src/repository.ts` 的玩家事务锁及创建配置草稿锁将返回值转为 `::text`，保留事务锁语义。提交 `7b7e734` 包含此修复；此前 `64424ed` 为 Prisma 错误日志改进。用户已在修复部署后成功创建角色并进入客栈。

2026-09-06 已修复同文件 `PostgresRepository.transition()` 的配置状态推进锁，将返回值同样转换为 `::text`，消除 DRAFT → TEST → PUBLISHED 路径的同类 Prisma void 反序列化风险。该修改已通过类型检查和现有配置发布/回滚自动化测试，仍需随下一次测试服部署完成 PostgreSQL 实环境发布验收。

## 6. 已验证范围与下一阶段验收

用户已实测的测试服流程：DEV 登录 → 创建角色 → 入住客栈 → 从出口进入白石街成功；玩家、NPC、建筑显示正常，北京时间/夜色、建筑交互与营业时间判断生效。深夜青丝美发室显示“店铺已打烊”属于预期结果。

2026-09-06 本地已补齐闭环验收入口：浏览器试玩支持退出当前会话并用同一测试账号重登；Cocos 客户端支持在美发室浏览发型、选择配色并付费应用，在服装店浏览款式、购买后穿戴，并可重新登录核对服务端存档。上述修改已通过 TypeScript 检查、自动化测试与后台生产构建，尚待推送及 Zeabur/Cocos 实际部署验收。

`docs/STATUS.md` 的历史自动化记录为 13 项测试、12 通过、1 项 PostgreSQL 集成测试跳过，覆盖 HTTP 买卖等规则。本次没有重跑这些测试；不能把这个历史结果升级为最新全量验证，也不能将自动化买卖通过等同于测试服人工闭环已完成。微信 Cocos 编译和真机验收尚无完成记录。

下一阶段按以下顺序推进，先完成闭环，再扩地图、任务与 NPC：

0. 任务 7：统一建筑入口和室内外切换体验。

1. 保持已通过的建角、客栈出口、白石街移动与建筑交互稳定；验证 NPC 初见及再次对话。
2. 完成商店买卖的测试服实际操作验收。初始配置基准：新角色 120 文，在街坊杂货铺花 12 文购买一袋鸣山大米，再在白石商行卖得 16 文，余额依次 120 → 108 → 124，米归零，账本应有赠款、买入、卖出记录。已发布配置若变更价格，应按该版本记录实际预期。
3. 在营业时段验证青丝美发室更换发型/发色、春衫衣坊购买及切换衣服，核对扣款、所有权、人物显示和账本。重复请求不得重复扣款。
4. 退出并以同一测试账号重登，确认位置、铜钱、背包、已拥有服饰、当前外观及 NPC 记忆持久保存；另行记录服务重启后的存档复验。不能仅凭 readyz 通过替代存档验收。
5. 用第二账号检查对方 Ghost 外观与最新保存装扮一致；核查后台配置发布的事务锁问题并验收配置发布/回滚。
6. 将失败步骤、复现条件、修复提交和复测结果写回本文件。闭环稳定后再增加地图、任务、NPC，并逐步推进赠礼、住宅、商铺和离线经营。

后续仍需安排：正式微信登录、Cocos 编译与微信真机操作、CDN/远程 Bundle/缓存与资源热更新、正式美术音效、性能和压力测试，以及生产备份恢复、告警和多管理员权限。当前资源版本清单存在，并不代表已经实现完整差分热更新。

## 7. 开发入口与维护规则

代码入口：

- `apps/server/src/app.ts`：登录、健康检查、API 路由。
- `apps/server/src/service.ts`：建角、移动、对话、交易、外观业务规则。
- `apps/server/src/repository.ts`：内存/数据库持久化、幂等、事务锁、配置版本与快照。
- `packages/game-config/index.ts`：初始世界、道路、NPC、建筑、商品和外观配置。
- `packages/game-rules/index.ts`：北京时间、营业判断、场景筛选与碰撞。
- `packages/client-runtime`、`apps/admin`、`apps/client-wechat`：共享交互与绘制、浏览器试玩/后台、Cocos 微信工程。
- `apps/worker/src/main.ts`：Redis/BullMQ 周期维护与健康检查。
- `database/prisma`、`database/seed.ts`：模型、正式 migration、初始世界 seed。重复 seed 不覆盖已经存在的世界版本。
- `docs/API.md`、`docs/WECHAT.md`、`docs/DEPLOYMENT.md`：接口、客户端与部署步骤，使用时结合本文件修正历史状态。

代码变更后按影响范围运行 `npm run typecheck`、`npm test`、`npm run build`。build 不包含 Cocos 引擎构建；PostgreSQL 集成测试需要独立测试数据库和 `TEST_DATABASE_URL`，未配置时会跳过。`dev:demo` 为重启清空的内存演示，不能用于验证测试服数据库持久化或代替 Zeabur API。

每完成重要阶段必须更新本文件，至少包括：北京时间日期、完成项、相关提交、测试环境与方式、通过/失败/跳过项、遗留问题和下一步。旧结论被新证据替代时，更新对应现状段落并保留必要变更记录。涉及实际云服务状态时记录核实时间与来源。

本文件只存非敏感交接信息。禁止写入数据库密码、带凭据的连接串、AppSecret、JWT_SECRET、SUBJECT_HASH_SECRET、ADMIN_TOKEN、访问令牌或包含这些值的日志。只允许提及环境变量名称和用途，不记录任何秘密值。

### 维护记录

- 2026-09-09：完成第一版通用商店面板。商品配置新增可选图标；场景接口返回商品定义；共享 `GameController.shopPanel()` 从当前建筑库存、玩家背包与服务端同步余额生成商品名称、图标、持有量、买入价、卖出价和单件交易视图。Web 游戏、微信小游戏及电脑调试工作台共用该数据模型，采用横阳小镇风格的非电商式商店面板；买卖仍调用既有服务端权威交易、Ledger 与幂等机制，未改交易业务。类型检查通过，`tests/game.test.ts` 与 `tests/client.test.ts` 共 30 项通过，三端构建通过；本地 API 重启后实测场景接口返回杂货铺、大米图标及 12/8 文价格。微信包已更新至 `dist/wechat-game`。

- 2026-09-09：完成白石街第一版最小任务面板。`/v1/quests` 改为遍历统一 Quest 配置并返回步骤进度；Quest 步骤支持配置标题与目标文案；共享 `GameController` 负责加载任务、依据服务端玩家账本生成当前步骤/目标/奖励/完成状态，Web 游戏、微信小游戏和电脑调试工作台均使用同一追踪模型。Web 与微信右上角提供可收起任务栏，横屏布局避开安全区；“第一桶金”完成后标记已完成及奖励已发放。类型检查通过，`tests/game.test.ts` 与 `tests/client.test.ts` 共 29 项通过，Web、调试工作台、微信小游戏构建均通过；本地页面与任务接口实测返回 200。线上测试服需部署本次 API/Quest 配置后才能获得完整目标文案。

- 2026-09-09：微信小游戏体验版根据真机反馈完成局部体验修复：主角与普通 NPC 放大、名字改为更清晰的细描边字形、对话/任务文字上移；NPC 碰撞框按实际人物高度校正；所有建筑门前交互区向公共道路侧扩大，避免触控摇杆下难以进入商店。`npm.cmd run typecheck` 通过，`tests/game.test.ts` 与 `tests/client.test.ts` 共 28 项通过，`npm.cmd run build:wechat-game` 通过；新包位于 `dist/wechat-game`，仍待重新导入微信开发者工具后实机复验。

- 2026-09-06：首次建立交接记录，核对 V2.2、现有文档和本地代码，整理用户已确认的测试部署与试玩进展，标注旧文档过时内容及后台状态推进锁待核查项。本次仅新增本文件并纳入 Git，不修改其他业务代码，不执行推送或部署。
- 2026-09-06：修复后台配置状态推进的 Prisma advisory lock 返回类型；浏览器试玩增加退出重登入口；Cocos 增加美发、服装购买/穿戴及重登验存档入口。`npm run typecheck` 通过；自动化测试 14 项中 13 项通过、1 项 PostgreSQL 集成测试因未配置 `TEST_DATABASE_URL` 跳过；`npm run build` 通过，Vite 报告后台主包约 990.50 kB 的体积提示，属于后续性能优化项。提交 `043cc93` 已推送并由 Zeabur 自动部署。
- 2026-09-06：公开测试服复验通过：`/healthz` 返回 200 和 `DEV`，`/readyz` 返回 200 和 ready，后台新构建包含退出重登功能。新建专用 DEV 测试账号完成建角、出客栈、买米、卖米和重登：余额 120 → 108 → 124，大米 0 → 1 → 0，重登仍在白石商行，基础外观与三笔账本保留。继续进入青丝美发室时，北京时间约 06:19，服务端按 08:00–20:30 规则返回 `CLOSED`，属于预期营业限制；美发和服装的测试服实操留待营业时段完成。


## 当前阶段交接摘要（2026-09-09）

### 已确定路线

- 主路线：Web First / TypeScript / Phaser / Canvas 2D；微信端仅保留平台必要的轻适配层。
- Cocos 已冻结归档，不再作为主开发链路；不维护第二套长期视觉版本。
- 当前只深做白石街，采用独立场景块、出口和拓扑连接，不做大型无缝地图。

### 任务 1～6 状态

1. 第一桶金任务闭环：已完成并通过 Web/服务端流程验收。
2. 白石街微信小游戏真机体验版：已完成构建与体验版验证；固定横屏、触摸、摇杆、入口、室内外、任务和交易可运行。
3. 真机体验问题修复：已完成主角/NPC显示、碰撞、输入兼容和入口交互的局部修复。
4. 最小任务面板：已完成，使用统一 Quest 配置驱动，支持第一桶金进度与完成状态。
5. 通用商店面板：已完成，Web/微信/后台共用商品视图，交易仍由服务端 Ledger 与幂等机制裁决。
6. 云存档与重登验收：已完成自动化 A/B 验收；身份、外观、金钱、背包、任务、场景、位置可恢复，重复领奖和重复出售被拒绝。

### 当前验收状态与遗留问题

- 存档、任务、商店及第一桶金自动化测试已通过；真实 PostgreSQL/Zeabur 重启后的人工存档复验仍需下一次测试服操作确认。
- 微信小游戏体验版可运行，但正式微信登录、生产凭据、分享、广告、支付、好友和排行榜尚未接入。
- 开发者工具中部分 `jsbridge`、`passive` 兼容提示属于平台/适配警告；关系值字段和关系玩法尚未实现，不在本阶段补做。

### 下一阶段顺序

7. 统一建筑入口和室内外切换体验。
8. 沉淀通用 NPC 站位、交互与对话入口。
9. 完善白石街场景资源、遮挡和移动体验。
10. 扩展配置驱动的任务与剧情内容。
11. 完成角色外观、服装店与美发室的统一体验验收。
12. 做白石街阶段性稳定性、性能和微信真机回归验收。

## 当前交互方向

2026-09-06 起，项目正式采用固定横屏开发与运行方案，以 16:9 为主要设计与适配基准。地图探索、NPC 互动、任务、剧情、商店、背包和关系界面统一在横屏内完成，不建立地图横屏/菜单竖屏的旋转方案。


## 当前阶段：白石街垂直切片

### Web / 微信客户端共用原则（2026-09-09 确认）

- Web/PC 端是场景、美术与交互组件的主实现；微信小游戏端原则上共用同一套场景数据、地图资源、人物/NPC 渲染、建筑、任务 UI、商店 UI、对话 UI 与业务流程。
- 微信端只保留平台必要的薄适配：固定横屏、安全区、Canvas/运行时兼容、触摸与虚拟摇杆、生命周期、网络请求及包体/加载策略。不得长期维护另一套简化的视觉、地图或玩法分支。
- 当前真机体验版的程序绘制场景与简化 UI 仅用于早期兼容性、输入与性能验证；白石街正式美术和 UI 完成后，应同步接入微信端并与 Web/PC 保持一致。
- 以后新增场景、人物、建筑、NPC、任务和界面时，优先扩展共享资源与组件；如确有微信平台差异，必须限定为适配层并说明原因，避免形成双线维护。

当前开发范围固定为白石街约 70%～80% 完成度的垂直切片和标准样板区，暂不同时铺开其他街区或大量空地图。阶段目标是跑通并验证：进入白石街、自由移动、镜头跟随、碰撞、建筑入口、室内外切换、NPC 站位与对话、任务触发与完成、奖励/关系变化、存档和重新进入后的状态恢复。

白石街同时承担标准制定职责：地图分层、建筑 Plot、室内场景、NPC、对话、任务、入口传送、碰撞、前景遮挡、固定 16:9 横屏适配、安全区、触控布局、资源加载、性能和存档兼容方案，均先在白石街验证后再沉淀复用。后续街区采用复用系统与配置/素材替换方式扩展。

手机适配优先覆盖常见横屏比例、全面屏和刘海/挖孔安全区；不通过简单拉伸解决适配，优先使用缩放、锚点、相机视口和响应式布局。新增美术或音频素材时，先列出素材需求和普通聊天模式生成命令，工作端负责接入、配置和测试。


## 白石街待处理问题

- 大树周围与地面连接处存在非透明边缘或抠图残留，当前已记录，暂不影响树干碰撞；后续由美术素材清理或替换解决。

### Web First 技术路线记录

- 2026-09-08：完成 Web First 技术审计并确定 Phaser + TypeScript 作为浏览器 Canvas 2D 日常开发主体；保留现有 Zeabur、PostgreSQL、Redis、Fastify API、Prisma、Worker、后台、共享规则和 Cocos 工程。新增独立 `apps/web-game`、平台抽象契约、WebPlatform 与 WeChatPlatform。Cocos 微信专属登录/请求调用已迁入适配器；未删除 Cocos、未改数据库结构、未改变 API 路径。`npm run assets:build`、`npm run typecheck` 与 `npm run build:game` 通过；微信 Cocos 引擎构建与真机验收待在编辑器中复测。

### 架构补充决策 2026-09-08

- Cocos Creator 正式退出日常主开发链路，冻结为归档及可复用素材、配置和历史实现来源；后续不与 Phaser/Web 客户端双线维护。
- Phaser + TypeScript + Web First 为唯一游戏客户端主路径；微信小游戏只保留薄平台适配层和阶段性真机验收职责。
- 横阳小镇采用“独立 Scene + 东南西北出口 + 拓扑连接”的拼图式世界，不建设大型无缝地图；长街可拆为多个方向一致的连续场景。
- 白石街后续出口规划：东至解放街、北至公桥路、南至九凰山；西侧待地理规划确认。九凰山为正式名称。
- 现有数据库、API、Zeabur、Prisma、Worker、后台和玩家数据继续保留，本决策不触发重建、清空或破坏性迁移。

- 2026-09-08：Web First 白石街第一组完成。Phaser 网页客户端迁入完整建角选项（男女各六种模板、肤色、发色、上下衣配色、四方向预览）和基础 HUD（场景/时间/铜钱、第一桶金任务状态、行囊、触摸方向键与互动按钮）。未修改服务端、数据库、交易或世界配置。`npm run build:game` 与 `npm run typecheck` 通过；本地页面 `http://127.0.0.1:5174/` 返回 200。

### 灵感文档使用规则

- 在较大开发阶段、功能设计节点和阶段里程碑，主动回看《富甲横阳小游戏开发思路和灵感整理》；小型 UI 修改和局部 Bug 修复不要求重复阅读全文。
- 灵感文档中的“以后考虑”“待验证”内容不得直接实现。若与当前阶段匹配，先向项目负责人说明对应灵感、采用理由、体验收益、工作量、架构/进度影响和时机建议，并明确询问“这条灵感现在正好可以融入当前开发，是否要采用？”，得到确认后再纳入任务。
- “已确定”原则可直接执行；开发中产生但暂不适合实施的新点子先建议记录，不打乱当前计划。
- 灵感文档负责保留想法，当前开发计划负责控制范围。
### Work 模式节省 AI 额度长期规则

- 默认增量开发：先读本文件，再按任务范围读取直接相关代码、配置、类型和测试；不做无必要的全项目扫描或体检。
- 小任务采用小范围、最小可行修改；同类需求集中处理，按修改范围选择测试，不重复分析已确定内容。
- 复杂架构、数据库、存档一致性、跨端、并发、性能或难定位 Bug 才进行深度推理；不为追求代码整洁主动重构或扩大需求。
- 发现额外问题先报告为建议，不未经确认实施；已有核心架构、安全、数据一致性和关键质量要求不能省略。
- 不整文件重写；重要阶段结束时只记录真正重要的完成情况、问题、状态和下一步。
- 新系统、新场景、新任务和里程碑按灵感文档规则回看；遇到“以后考虑”或“待验证”内容先向项目负责人说明并征求确认。
- 默认输出“完成内容 / 关键文件 / 测试结果 / 风险或下一步”，保持简洁。
- 技术选择优先依赖少、工具少、结构清楚、长期易维护。
- 2026-09-09：新增 `apps/wechat-game` 微信小游戏最小真机体验构建目标。它复用 Phaser/Web First 的 `GameController`、白石街、NPC、碰撞、入口、室内、第一桶金与服务端交易，不改任务/经济业务。输出包采用微信官方快速启动模板的 Canvas 适配器、`game.json` 固定横屏、Canvas 内安全区 HUD、虚拟摇杆、互动/买卖按钮和 FPS 显示；构建命令为 `npm.cmd run build:wechat-game`，输出为被忽略的 `dist/wechat-game`。类型检查、微信构建及第一桶金相关 4 项自动化测试通过。开发者工具与真机验收尚需用户使用真实 AppID、测试服合法域名和手机预览完成；正式微信登录、分享、广告、支付、好友和生产服务仍未接入。
- 2026-09-09：完成白石街云存档 A/B 验收测试。新增端到端用例覆盖同一测试账号建角、进入白石街、接取第一桶金、买米后退出重登，以及卖米领奖后再次退出重登；核对身份、外观、余额、背包、任务步骤、场景和位置。验证同一交易请求重试只返回同一结果，新请求重复出售因库存不足被拒绝，任务奖励账本仅存在一笔，重登后任务保持已完成。`npm run typecheck` 与相关 31 项服务端/客户端测试全部通过。当前 PlayerState 尚未包含关系值字段，本次未新增关系玩法；关系值验收记录为当前功能不适用。

- 2026-09-09：完成任务 6 后的 Git 基线落盘。
pm.cmd run typecheck 通过；定向测试 31 项通过、1 项 PostgreSQL 集成测试因未配置测试数据库跳过、0 项失败。main 已将 20c74ad 与 648f4e3 推送至 origin/main，工作区保持干净。下一任务为任务 7：统一建筑入口和室内外切换体验。
- 2026-09-10：任务 9 完成最小 NPC/商业关系反馈。当前不存在独立关系值或好感度字段；复用已持久化的 metNpcs 与账本记录，在首次交谈显示“已认识”，首次商店交易显示商号往来提示。未修改服务端、数据库或复杂 UI。类型检查通过，相关客户端/服务端测试 32 项通过、1 项 PostgreSQL 集成测试跳过。任务 8 的素材接入暂缓，不阻塞后续任务。
- 2026-09-10：已固化白石街角色、美术与对话表现规范，明确统一视觉基准、白石商行样板、主角/NPC 造型、三级对话体系及 Web/微信资源共用原则。详见 [《富甲横阳·白石街角色、美术与对话表现规范 V1》](docs/BAISHI_ART_CHARACTER_DIALOGUE_SPEC.md)。
- 2026-09-10：白石街正式美术样板方向进一步确认：优先制作白石商行、年轻男伙计及其半身对话立绘，再制作主角正式 Q 版基础样板；样板验证后扩展其他建筑与 NPC。规范见 [白石街角色、美术与对话表现规范](docs/BAISHI_ART_CHARACTER_DIALOGUE_SPEC.md)。
